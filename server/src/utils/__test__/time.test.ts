// import { getTimeStamp, wait } from "./time";

describe("getTimeStamp and wait functions", () => {
  jest.useFakeTimers();

  describe("getTimeStamp function", () => {
    it("should return a string", async () => {
      // TODO: Fix this test
      // const timeStamp = await getTimeStamp();
      // expect(typeof timeStamp).toBe('string');
    });

    it("should return a base-36 encoded timestamp", async () => {
      // TODO: Fix this test
      // const timeStamp = await getTimeStamp();
      // expect(timeStamp).toMatch(/^[0-9a-z]+$/);
    });
  });

  describe("wait function", () => {
    it("should resolve after specified time", async () => {
      // TODO: Fix this test
      // const ms = 1000;
      // const promise = wait(ms);
      // jest.advanceTimersByTime(ms);
      // await promise;
      // expect(setTimeout).toHaveBeenCalledTimes(1);
      // expect(setTimeout).toHaveBeenLastCalledWith(expect.any(Function), ms);
    });
  });
});
